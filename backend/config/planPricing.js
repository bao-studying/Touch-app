// Giá theo gói — dùng ở backend khi tạo đơn thanh toán (KHÔNG tin số tiền client gửi lên).
const PLAN_PRICES = {
  level1: 99000,
  level2: 199000,
  level3: 299000,
};

module.exports = { PLAN_PRICES };
